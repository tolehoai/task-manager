import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  taskList: {
    margin: "30px 10px",
  },
  name: {
    fontSize: "30px",
    fontWeight: "800",
    color: "#1a2e57",
    marginBottom: "15px",
  },
});
export { useStyles };
